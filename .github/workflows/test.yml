name: Run tests

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        julia-version: ['1', '^1.10.0-rc1', 'nightly']
        os: [ubuntu-latest, windows-latest, macOS-latest]
        arch: [x64, x86]
        exclude:
          - os: macOS-latest
            arch: x86
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v1.9.3
        with:
          version: ${{ matrix.julia-version }}
      - uses: julia-actions/cache@4b5eb1920c6b12d13fa285405fa97ebace83f328
      - uses: julia-actions/julia-runtest@v1
        with:
          coverage: 'false'
      - uses: julia-actions/julia-processcoverage@v1
